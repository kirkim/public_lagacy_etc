extends layout.pug

block content
	if errorMessage
		span=errorMessage
	form(method="POST")
		input(placeholder="Username", type="text", name="username")
		input(placeholder="password", type="password", name="password")
		input(type="submit", value="Login")
	hr
	b create new ID&nbsp;
		a(href="/join") Create &rarr;
